// Programa que eleva un numero al cuadrado

main {
  int x, y, resultado;

  scanf x; // Esta linea lee el valor de x
  scanf y;
  resultado = x^y; // Esta linea calcula el cuadrado de x
  printf resultado; // Esta linea imprime el resultado

  scanf x; // Esta linea lee el valor de x
  scanf y;
  resultado = x^y; // Esta linea calcula el cuadrado de x
  printf resultado; // Esta linea imprime el resultado
  
}